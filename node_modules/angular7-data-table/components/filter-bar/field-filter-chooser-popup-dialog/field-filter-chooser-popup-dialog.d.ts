import { OnInit, QueryList } from '@angular/core';
import { DataTableTranslations } from '../../../types/data-table-translations.type';
import { FilterableField } from '../../../types/filterable-field.type';
import { ListKeyManager } from '@angular/cdk/a11y';
interface DialogData {
    fields: FilterableField[];
    labels: DataTableTranslations;
}
export declare class FieldFilterChooserPopupDialog implements OnInit {
    private _dialogCloser;
    data: DialogData;
    listItem: QueryList<any>;
    keyboardEventsManager: ListKeyManager<any>;
    activeItem: any;
    fieldSearchQuery: string;
    filteredFields: FilterableField[];
    constructor(_dialogCloser: (dialogResult?: any) => void, data: DialogData);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    onFieldSearch(searchQuery: string): void;
    fieldSelected(field: FilterableField): void;
    handleKeyDown(event: KeyboardEvent): boolean;
}
export {};

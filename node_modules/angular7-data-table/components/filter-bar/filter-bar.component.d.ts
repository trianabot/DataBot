import { OnInit } from '@angular/core';
import { PopupDialogService } from 'ngx-popup-dialog';
import { Filter } from '../../types/filter.type';
import { FilterableField } from '../../types/filterable-field.type';
import { DataTableComponent } from '../table/table.component';
import { MatDialogRef } from '@angular/material/dialog';
import { PopupDialog } from 'ngx-popup-dialog/lib/popup-dialog/popup-dialog';
export declare class FilterBarComponent implements OnInit {
    dataTable: DataTableComponent;
    popupDialogService: PopupDialogService;
    fieldChooserDialogRef: MatDialogRef<PopupDialog>;
    filters: Filter[];
    fields: FilterableField[];
    constructor(dataTable: DataTableComponent, popupDialogService: PopupDialogService);
    ngOnInit(): void;
    inputFocused(event: Event): void;
    chipClicked(event: Event, filter: Filter): void;
    openFieldChooserDialog(triggeredElement: any): void;
    openFieldFilterDialog(triggeredElement: any, filter?: Filter): MatDialogRef<PopupDialog, any>;
    removeFilter(filter: Filter): void;
    formatValue(filter: Filter): any;
    formatChipDisplayText(filter: Filter): string;
    private convertFilterToEmittedFilter;
}

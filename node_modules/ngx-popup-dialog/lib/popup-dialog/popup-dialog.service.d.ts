import { Overlay } from '@angular/cdk/overlay';
import { ComponentType } from '@angular/cdk/portal';
import { TemplateRef } from '@angular/core';
import { MatDialog, MatDialogRef } from '@angular/material/dialog';
import { Direction, PopupDialog } from './popup-dialog';
export interface PopupDialogServiceConfig {
    direction?: Direction;
    coverTriggeringElement?: boolean;
    scaleToTopOnBottomOverflow?: boolean;
    maxWidth?: number | string;
    maxHeight?: number | string;
    suppressCloseOnClickSelectors?: string | string[];
}
export interface PopupDialogConfig extends PopupDialogServiceConfig {
    data?: any;
}
export declare class PopupDialogService {
    private _overlay;
    private _dialogService;
    constructor(_overlay: Overlay, _dialogService: MatDialog);
    open<T>(componentOrTemplateRef: ComponentType<T> | TemplateRef<T>, triggeringElement: any, config?: PopupDialogConfig): MatDialogRef<PopupDialog>;
}
